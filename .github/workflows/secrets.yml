name: Secrets check

on:
  push:

jobs:
  runs-on: ubuntu-latest

  steps:
    - name: Show Scerets Usage
      env: 
        APP_ENV: ${{ secrets.APP_ENV }}
      run: |
        echo "Application Environment Loaded"

